import Image from "next/image";

const AboutSection = () => {
    return (
        <section className="about-section-container">
            
        </section>
    );
};

export default AboutSection;